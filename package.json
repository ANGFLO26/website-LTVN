{
  "name": "lt-vietnam-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:admin": "npm --prefix admin run dev",
    "dev:api": "npm --prefix api run start:dev",
    "build": "npm run check:i18n && tsc -b && vite build",
    "build:admin": "npm --prefix admin run build",
    "build:api": "npm --prefix api run build",
    "start:api": "npm --prefix api run start:prod",
    "db:generate": "npm --prefix api run db:generate",
    "db:push": "npm --prefix api run db:push",
    "db:migrate": "npm --prefix api run db:migrate",
    "db:studio": "npm --prefix api run db:studio",
    "db:seed": "npm --prefix api run build && npm --prefix api run db:seed",
    "check:i18n": "node scripts/validate-i18n.mjs",
    "lint": "oxlint src scripts vite.config.ts",
    "preview": "vite preview",
    "test:smoke": "node scripts/smoke-test.mjs",
    "test:visual": "node scripts/phase1-visual-check.mjs",
    "test:phase2": "node scripts/phase2-product-check.mjs",
    "test:phase3": "node scripts/phase3-content-check.mjs",
    "test:admin-drizzle": "node scripts/verify-drizzle-admin.mjs"
  },
  "dependencies": {
    "lucide-react": "^1.33.0",
    "playwright": "^1.56.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-router": "^8.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "oxlint": "^1.75.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
